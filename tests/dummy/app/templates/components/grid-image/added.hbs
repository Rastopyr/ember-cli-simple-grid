{{input type="number" value=columns}}

<button {{action add}}>
  Add item to start
</button>

<div>
  {{#simple-grid columnWidth=300 columns=columns as | grid |}}
    {{#each items as | item index |}}
      {{#grid.image index=index}}
        <img src="{{item.image}}" alt="">
      {{/grid.image}}
    {{/each}}
  {{/simple-grid}}
</div>

{{yield}}
