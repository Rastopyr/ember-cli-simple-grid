{{yield (hash
  item=(component 'simple-grid/item'
    gutter=gutter
    columnWidth=columnWidth
    columns=columns
    placeItem=(action placeItem)
  )

  image=(component 'simple-grid/image-item'
    gutter=gutter
    columnWidth=columnWidth
    columns=columns
    placeItem=(action placeItem)
    replaceItems=(action fireRerender)
  )
)}}
